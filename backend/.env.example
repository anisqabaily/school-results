# عدل القيم بما يناسب بيئتك المحلية قبل تشغيل الخادم
DB_HOST=127.0.0.1
DB_USER=sa
DB_PASSWORD=your_db_password
DB_NAME=info_all
DB_DIALECT=mssql
# إذا كنت تحتاج إلى instance name على SQL Server ضع هنا (اختياري)
# MSSQL_INSTANCE=SQLEXPRESS
JWT_SECRET=replace_with_a_strong_secret
PORT=4000
