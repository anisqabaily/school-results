````markdown
```markdown
# واجهة المستخدم (React)
- تعتمد على MUI لتصميم واجهة حديثة.
- هناك شاشة تسجيل دخول ثم صفحة رئيسية تحتوي على Tabs (مدارس، معاهد، تعديل مدارس، تعديل معاهد، ملف المستخدم).
- عند البحث يتم استدعاء /api/students/search أو /api/mahd/search.
- عند اختيار طالب يمكن الضغط على "طباعة" لاستدعاء /api/students/:id/print وفتح الناتج في تبويب جديد أو تنزيل PDF.
```
````
